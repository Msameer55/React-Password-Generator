# React-Password-Generator
I have built the password generator app using react where user can generate the password, users have the options to generate the password using only numbers, or password could also be varchar and user can also copy the password.
